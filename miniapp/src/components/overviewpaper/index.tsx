import { Text, View } from '@tarojs/components';
import { PropsWithChildren, useEffect, useState } from 'react'

const OverviewPaper = (props: PropsWithChildren) => {
  useEffect(() => { }, [])
  return (
    <View>
      <Text>Overview Paper</Text>
    </View>
  );
};

export default OverviewPaper;